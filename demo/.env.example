# Upstash Redis (challenge + session store). Create at https://console.upstash.com
UPSTASH_REDIS_REST_URL="https://example-12345.upstash.io"
UPSTASH_REDIS_REST_TOKEN="replace-me"

# Exact origin the browser loads. Signed into every challenge and enforced on every
# state-changing request, so it must match what the browser actually sends.
APP_ORIGIN="http://localhost:3000"

# mainnet | testnet | devnet | localnet
SUI_NETWORK="mainnet"

# Optional. Overrides the public fullnode gRPC endpoint derived from SUI_NETWORK.
# SUI_RPC_URL="https://fullnode.mainnet.sui.io:443"

# Namespace offchain-manager API key. Generate one at https://dev.namespace.ninja
# Read only by server-only modules; never sent to the browser or the gateway.
NAMESPACE_API_KEY="replace-me"

# Optional. Mainnet Ethereum RPC used for the CCIP-Read ENS resolution preview.
# viem's built-in public default is rate-limited; set a real provider (Alchemy,
# Infura, etc.) for reliable previews.
# MAINNET_RPC_URL="https://eth-mainnet.g.alchemy.com/v2/replace-me"
